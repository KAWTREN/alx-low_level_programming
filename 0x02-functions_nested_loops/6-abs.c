#include "main.h"
#include <stdio.h>
/**
 *_abs-computes the absolute value
 *
 * @n: number to  computes the absolute value
 *
 * Return: int n
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n == 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}

